# 購物平台專案規格文件

## 專案概述
仿 momo 風格的購物網站,作為求職作品集專案。包含會員前台 + 管理後台。

## 技術棧

### 後端
- Spring Boot
- Spring Security + JWT(會員驗證、角色權限)
- 資料庫:(依實際使用填入,如 MySQL / PostgreSQL)

### 前端
- Vue3(Vite)
- Pinia(狀態管理)
- Vue Router
- Axios

### 其他
- 付款流程為模擬假付款,非串接真實第三方金流

---

## 功能模組

### 會員端(前台)
- 首頁:輪播 banner、分類導覽、熱銷/推薦商品
- 商品列表:分類篩選、價格區間篩選、排序
- 商品詳情頁:規格選擇、數量選擇、加入購物車
- 購物車
- 結帳流程:收件資訊 → 付款方式 → 訂單確認
- 會員中心:訂單查詢、個人資料、收件地址管理
- 登入 / 註冊

### 後台管理(Admin)
- 商品管理:CRUD、上下架、庫存管理
- 訂單管理:狀態流轉(待付款 → 已付款 → 出貨中 → 已完成 / 取消)
- 會員管理
- 分類管理
- 簡易銷售報表

---

## 資料庫表設計(核心)

| 表名 | 說明 |
|---|---|
| `member` | 會員資料 |
| `admin` | 管理員帳號 |
| `category` | 商品分類(可設計成多層結構) |
| `product` | 商品主檔 |
| `product_sku` | 規格 / 庫存(多規格商品用) |
| `cart_item` | 購物車項目 |
| `orders` | 訂單主檔 |
| `order_item` | 訂單明細 |
| `address` | 會員收件地址 |

> 欄位細節(型別、關聯、索引)可在開發資料庫 schema 時再進一步展開。

---

## 建議開發順序

1. **後端基礎架構**:Spring Security + JWT 會員驗證打通
2. **商品 CRUD**:先做後台管理,再串前台列表 / 詳情頁
3. **購物車功能**:先用資料庫儲存(不引入 Redis)
4. **訂單流程**:下單 → 模擬付款 → 狀態變更
5. **前台頁面串接**:Vue3 組件化開發
6. **後台管理頁面**開發

---

## 給 AI 編碼助手的溝通建議

跟 code 模式(如 Claude Code、Cursor 等)溝通時,建議:

1. **先丟這份文件當作專案背景**,讓它知道整體架構與目標
2. **一次只聚焦一個模組**,例如:「先幫我用 Spring Boot 建立 `member` 和 `admin` 的 Entity + Repository + Security 設定」,不要一次要求整包產出
3. **明確指定技術細節**,例如資料庫種類、是否要用 Lombok、DTO 命名慣例等,減少它自行猜測
4. **要求它先列出檔案結構或 API 清單再動手寫**,方便你確認方向是否正確,再進入實作
5. **每完成一個模組就簡單測試 / review**,避免累積到後面才發現架構問題要大改

